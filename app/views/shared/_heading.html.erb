<div class="main-heading">
  <% if defined?(icon) %>
      <span class="card-img-top palette-<%= icon[:color] %>"><%= fa_icon icon[:name], class: 'fa-5x' %></span>
  <% end %>
  <h1><%= fa_icon 'quote-left' %> <%= h1 %> <%= fa_icon 'quote-right'%></h1>
  <p class="lead"><%= p %>.</p>
</div>

