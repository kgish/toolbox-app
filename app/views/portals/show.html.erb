<div class="main-heading">
  <span class="card-img-top palette-<%= @tool.color %>"><%= fa_icon @tool.icon, class: 'fa-5x' %></span>
  <h1><%= @tool.title %></h1>
  <p class="lead"><%= @tool.text %></p>
</div>

<div class="main-content"><%= @tool.content %></div>
